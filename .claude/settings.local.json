{
  "permissions": {
    "allow": [
      "Bash(cat:*)",
      "Bash(npx tsc:*)",
      "WebSearch",
      "WebFetch(domain:supabase.com)",
      "WebFetch(domain:github.com)",
      "Bash(npx vitest:*)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); echo \"$input\" | python3 -c \"import sys,json; d=json.load(sys.stdin); exit(0 if 'git commit' in d.get('tool_input',{}).get('command','') else 1)\" && echo '{\"systemMessage\": \"Pre-commit : /Tests (tests unitaires) · /Docs (documentation feature)\"}' || true",
            "shell": "bash"
          },
          {
            "type": "command",
            "command": "input=$(cat); echo \"$input\" | python3 -c \"import sys,json; d=json.load(sys.stdin); exit(0 if 'git push' in d.get('tool_input',{}).get('command','') else 1)\" && echo '{\"systemMessage\": \"Rappel : lancer /Security avant de pusher en prod\"}' || true",
            "shell": "bash"
          }
        ]
      }
    ]
  }
}
